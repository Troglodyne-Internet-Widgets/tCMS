*.swp
*.swo
# A site operator dropping their own favicon at the repo root -- 6a34784.
# Anchored, or it also swallows the default we ship in www/img/icon.
/favicon.ico
dist/
www/assets
data/
blib/
pm_to_blib
pod2htmd.tmp
www/scripts/list.min.json
www/scripts/highlight.min.js
www/scripts/chart.js
www/scripts/reveal.js
www/styles/obsidian.min.css
www/styles/reveal.css
www/styles/reveal-white.css
www/.well_known
config/auth.db
config/has_users
# The vault key, when bin/tcms-vault-key --file put one here rather than the
# service handing one over.  Committing it would put the key in the same place
# as everything it protects, which is the one thing it exists not to be.
config/secrets.key
config/main.cfg
config/setup
Makefile
MYMETA.yml
MYMETA.json
node_modules/
www/statics/
totp/
fail2ban/tcms-jail.conf
logs/
run/
*-shm
*-wal
.xslate_cache/
